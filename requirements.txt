# LIKU Python Dependencies
# 
# RECOMMENDED: Install using pyproject.toml instead:
#   pip install -e .              # Install in editable mode
#   pip install -e ".[dev]"       # Install with dev dependencies
#
# This file is kept for backward compatibility.
# Python 3.9+ required

# Core dependencies
psutil>=5.9.0  # For process management and orphaned pane detection
textual>=0.60.0  # For TUI dashboard
pygetwindow>=0.0.9
pywin32>=306
playwright>=1.40.0 # For browser automation and web inspection
google-auth>=2.20.0 # For authentication
google-generativeai>=0.5.0 # For Gemini API